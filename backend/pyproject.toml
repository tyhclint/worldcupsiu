[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "httpx>=0.28.1",
    "pytest>=9.0.3",
    "python-dotenv>=1.2.2",
    "supabase>=2.30.0",
    "uvicorn>=0.47.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
